<UserControl x:Class="ParkingManagement.View.UserControls.CardCustom"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ParkingManagement.View.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <Grid>
            <!--<Border Name="bdGrid" BorderBrush="LightGray" Margin="2" BorderThickness="1" CornerRadius="10">
                <Border.Effect>
                    <DropShadowEffect/>
                </Border.Effect>
                <Grid Width="{Binding ElementName=bdGrid}"></Grid>
            </Border>-->
            <Border CornerRadius="8" Background="White" Margin="0,0,133,91">
                <Border.Effect>
                    <DropShadowEffect ShadowDepth="2" Opacity="0.2"/>
                </Border.Effect>
            </Border>
        </Grid>
    </Grid>
</UserControl>
